-- Seed default message templates for every org on first use (we’ll also create them in-app).
-- This seed is optional; it helps demos.

-- No global inserts here because org_id is tenant-specific.
-- We will seed per-org in the app during onboarding.
